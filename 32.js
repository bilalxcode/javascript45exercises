const current_users = ['bilal', 'bilal123', 'bilalxyz', 'bilal098', 'BILAL']

const new_users = ['john', 'johndoe', 'bilal123', 'tina432', 'bilal098']
for (let index = 0; index < current_users.length; index++) {

    if (current_users[index] == new_users == [index]) {
        console.log("enter a new username")
    }
    else {
        console.log("Username is available ")
    }
}