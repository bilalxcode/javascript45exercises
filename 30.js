const username = ['admin', 'manager', 'user', 'eric', 'tina']

for (let index = 0; index < array.length; index++) {
    if (username[index]) {
        console.log("Hello admin, would you like to see a status report?")
    }
    else if (username[index]) {
        console.log("Hello manager, thank you for logging in again.")
    }
    else if (username[index]) {
        console.log("Hello user, thank you for logging in again.")
    }
    else if (username[index]) {
        console.log("Hello Eric, thank you for logging in again.")
    }
    else if (username[index]) {
        console.log("Hello Tina, thank you for logging in again.")
    }
    else {
        console.log("Exit")
    }
}